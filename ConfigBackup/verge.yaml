# Clash Verge Rev GUI 配置变更说明（不是可直接覆盖的完整配置）。
# 部署脚本读取目标电脑现有 verge.yaml，只更新这些顶层字段。
enable_system_proxy: false
clash_core: verge-mihomo
verge_mixed_port: 7897

# enable_tun_mode 由 -TunMode Auto|Enable|Disable 控制；Auto 不修改现值。
